# CONTEXT

How much context an agent carries here and where the rules come from. The
router and lawbook are the incumbents; silent compaction and an inlined
lawbook are refused by name.
PROBE (tier: probe, E7): `contracts validate --type context-contract` judges it.

```yaml context-contract
version: "0.1"
router: AGENTS.md
budget_lines: 200
compaction: banked
lawbook: on-demand
```
